Run pip install -r requirements.txt

Then run main.py. This will run the rapper classification task, then the lyrics generation task and use the saved lyrics generation model for a demo which generates 4 lyrics for every rapper.
